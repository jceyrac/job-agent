"""Board scrapers — query-driven, broad-net aggregation (LinkedIn, Indeed, etc.).
These scrapers are parameterised by search queries and return all matching jobs
without pre-filtering for relevance.  Scorer is the sole relevance decision point.

See constitution Principle I (Filet large) and Principle VIII (Scraper taxonomy).
"""
